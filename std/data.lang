
import "std:io";

func map(list, fn) {

	var len = list.length();

	for (var i in 0..len) {
		var b = list[i];
		var a = fn(b);
		list[i] = a;
	}

}

var x = [ 5..20 ]; 

func cb(entry) {
	return (entry ** 2); 
}

var mapped = map(x, cb);

println(mapped);